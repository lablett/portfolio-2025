/**
 * BackgroundScanline - Animated scanline layer (layer 3)
 * Renders animated horizontal or vertical scanline effects
 */
const BackgroundScanline = ({ type }) => (
  <div className={`section-bg-scanline section-bg-scanline-${type}`} />
)

export default BackgroundScanline
